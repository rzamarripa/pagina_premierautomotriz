# Configuración de correo electrónico
EMAIL_USER=tu_correo@dominio.com
EMAIL_PASS=tu_contraseña_smtp

# reCAPTCHA (opcional)
RECAPTCHA_SECRET_KEY=tu_recaptcha_secret_key

# Configuración de puertos
PREMIER_PORT=3006
LANDING_PORT=8081

# Configuración de base de datos (si se usa en el futuro)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=premierautomotriz
DB_USER=usuario_db
DB_PASS=contraseña_db

# Configuración de Redis (si se usa en el futuro)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASS=

# Configuración de JWT (si se usa en el futuro)
JWT_SECRET=tu_jwt_secret_muy_largo_y_seguro

# Configuración de CORS
CORS_ORIGIN=http://localhost:8081

# Configuración de logging
LOG_LEVEL=info
LOG_FILE=logs/app.log
